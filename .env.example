# Async PostgreSQL connection string used by the API and Alembic.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/macro_tracker_test
# Runtime environment name, such as development or production.
APP_ENV=development
# IANA timezone used when a user's timezone is not supplied by the CLI.
DEFAULT_TIMEZONE=America/Sao_Paulo
# Application log verbosity.
LOG_LEVEL=INFO
# Public origin used by OAuth issuer and MCP metadata URLs.
PUBLIC_BASE_URL=http://localhost:8000
# Frontend origin for browser OAuth redirects; falls back to PUBLIC_BASE_URL when empty.
WEB_BASE_URL=
# HMAC key for browser CSRF tokens. Required in production; use a long random value.
SECRET_KEY=development-only-secret-key-change-me
# Google OAuth client credentials (required for production human login).
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# Comma-separated emails allowed to create new accounts.
# ALLOWED_EMAILS=
# Show the OAuth consent page after Google login.
OAUTH_REQUIRE_CONSENT=true
# Comma-separated enabled remote food providers (for example: usda,off,tbca,fatsecret).
# FOOD_PROVIDER_SOURCES=
# Free USDA FoodData Central API key from https://api.data.gov/ (P2).
# USDA_FDC_API_KEY=
# User-Agent required by Open Food Facts.
# OFF_USER_AGENT=macro-tracker/0.1 (https://github.com/KaueBenk/macro-tracker)
# Maximum time for a remote provider search or barcode lookup.
# PROVIDER_TIMEOUT_SECONDS=5.0
# Maximum TBCA detail pages fetched for one search.
# TBCA_DETAIL_LIMIT=5
# FatSecret Basic API credentials (provider remains disabled when either is empty).
# FATSECRET_CLIENT_ID=
# FATSECRET_CLIENT_SECRET=
# Maximum FatSecret detail pages fetched for one search.
# FATSECRET_DETAIL_LIMIT=5
# Development-only email used to resolve an existing OAuth user.
# DEV_LOGIN_EMAIL=you@example.com
# Use NullPool and disable asyncpg statement caching for serverless/pgbouncer.
# Vercel auto-detects this from VERCEL when SERVERLESS is omitted.
SERVERLESS=false
