import { PlaceholderPage } from "@/components/placeholder-page";

export default function FoodsPage() {
  // Placeholder until food search and CRUD are migrated from the Jinja GUI.
  return <PlaceholderPage title="Alimentos" />;
}
