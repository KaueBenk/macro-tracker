import { PlaceholderPage } from "@/components/placeholder-page";

export default function AccountPage() {
  // Placeholder until account settings are migrated from the Jinja GUI.
  return <PlaceholderPage title="Conta" />;
}
