import { PlaceholderPage } from "@/components/placeholder-page";

export default function HistoryPage() {
  // Placeholder until range summaries are migrated from the Jinja GUI.
  return <PlaceholderPage title="Histórico" />;
}
