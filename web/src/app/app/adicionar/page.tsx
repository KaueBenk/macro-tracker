import { PlaceholderPage } from "@/components/placeholder-page";

export default function AddPage() {
  // Placeholder until the registration flow is migrated from the Jinja GUI.
  return <PlaceholderPage title="Registrar entrada" />;
}
